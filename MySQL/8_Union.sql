-- Description: UNION and UNION ALL
/*
Used to combine result set of two or more select queries
For UNION an UNION ALL to work, oreder of fields in both select statement should be same.

UNION removes duplicates. UNION ALL does not remove duplicate.

Sorting results of UNION and UNION ALL
-- To sort results of UNION an UNION ALL query, ORDER BY clause should be used only on the last SELECT statement in the UNION query.


UNION combines rows from two tables while JOIN combines columns of two tables

*/
